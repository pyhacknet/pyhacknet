# import os
# import sys
#
# BASE_PATH = os.path.dirname(os.path.realpath(__file__))
#
#
# directories = [x[0] for x in os.walk(BASE_PATH)]
# for directory in directories:
#     sys.path.append(directory)
